# Snapszli — app folder

**Live:** https://TomNslg.github.io/snapszli/  
**Reopen docs:** `../maintenance.md` · Pages: `GITHUB-PAGES.md`

Dev server:

```bash
python3 -m http.server 8765 --bind 0.0.0.0
```

Push updates from this folder (own git repo) to `origin/main`; bump `CACHE` in `sw.js` when shipping shell changes.
