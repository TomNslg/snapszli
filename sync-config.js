/** Public sync settings — token is entered once in the app (stored in localStorage). */
window.SNAPSZLI_SYNC_CONFIG = {
  enabled: true,
  repo: "TomNslg/snapszli",
  branch: "main",
  dataPath: "data/devices",
  token: "",
};
